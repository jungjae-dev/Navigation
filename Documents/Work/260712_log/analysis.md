# 260710 현장 로그 분석 (분석일 260712)

수집: 지하주차장 1곳 (번호 단독 "N" 스켈레톤, 층 별도 입력 B1, LiDAR 기기)
세션 3건: find 28s / scan 10s / find 23s → `NavigationTests/ParkingFinder/Fixtures/`에 픽스처 커밋됨

## 검증된 것 ✅

| 항목 | 근거 |
|---|---|
| 스캔 자동 저장 (FR-002) | "09" 15회 인식 → 저장, 층 시트 수동 입력(FR-005) 작동 |
| 오검출 방어 (FR-008, SC-006) | 안내문("주차금지구역" 등) low-conf 기각 다수, 번호판 "12아 2775" 변형 전부 기각, 단독 문자 skeleton-mismatch 기각 — **격자 오염 0건** |
| 상태 전환 (FR-009) | 두 find 세션 모두 searching → 축 안내(관측 2개) 정상, targetEst 수렴 |
| anchorFailing 배너 (FR-015) | raycast 연속 5회 실패 시점에 발동 |
| 레코더/내보내기 (DR-003) | 스키마 정합, 리플레이 재현 성공 |

## 발견 → 수정 완료 🔴

**선행 0 표기 흔들림**: 같은 표지판이 "09"/"9", "03"/"3"으로 분리 관측. 목표 "09" 저장 시 "9" 인식으로는 도착 확정 불가.
→ 커밋 77d9a46: ① normalize 선행 0 제거 ② 도착 판정 원문+파싱 동치 이중화.

## 관찰 → 추적 항목 ⚠️

1. **raycast 실패율 높음**: 세션3에서 10/16 (LiDAR 기기임에도). 주차면 번호판이 기둥 수직면이 아닌 낮은 위치/바닥 각도라 vertical raycast가 빗나가는 것으로 추정. → T045 후보: `.vertical` 우선 → `.any` 폴백 순서 재검토 또는 즉시 `.any`. 다음 현장에서 데이터 추가 수집
2. **N 스켈레톤의 허용 범위**: 번호 단독 체계에선 아무 1~3자리 숫자가 템플릿 통과("27","120","707" 관측 진입, hit<2로 확정 차단됨). 현재 방어선 = 3자리 제한 + hit 2회 확정. 오확정 사례 발생 시 강화 검토
3. **도착 미확정**: 두 find 세션 모두 목표 직접 인식 없이 screen-exit 종료 — 사용자가 목표 기둥 앞까지 안 갔거나 선행 0 버그 영향. 수정 후 재검증 필요 (D4)

## 개선 백로그 (260712 논의)

4. **번호 그라디언트 안내**: 번호 단독(N) 주차장에서 강등 시 "목표 9번 — 번호가 작아지는 방향으로" 문구 추가. 번호는 경로를 따라 단조 증가하므로 접힘(스네이크)에 강건한 유일한 신호. **도입 완료** (NumberGradientHint + 테스트 7건)
5. **bbox 크기 하한 필터**: 화면 비율 N% 미만 텍스트는 OCR 후보에서 제외 — 원거리 오인식 차단 + raycast 실패 예정 관측 낭비 방지. 임계값은 다음 현장 로그에서 "작은 bbox ↔ 오인식/raycast 실패" 상관 확인 후 결정 (T045)

## 다음 현장에서 확인할 것

- 화살표 방향 부호 (D2 — 미검증)
- 도착 확정 (D4 — 수정 후 재시도)
- 강등 시나리오 (D3 — 이번 로그엔 잔차 초과 미발생, residual 0.0 유지)
- 다른 코드 체계 주차장 (F-Z-N 등) 수집 (T047)
