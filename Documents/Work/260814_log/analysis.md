# 260801 현장 로그 분석 (분석일 260814)

수집: 지하주차장 2곳째 — **구역+번호 결합(ZN) 체계** ("D5"), 층 별도 입력 B3, LiDAR 기기
세션 3건: scan 10s / scan 9s / **find 70s (최초 도착 확정 성공)** → find 세션 픽스처 커밋(b0915fe)

## 🎉 검증된 것 (이번에 처음 확인)

| 항목 | 근거 |
|---|---|
| **도착 확정 (D4, FR-013)** | 70.4s `격자 안내 → arrived (target 2 consecutive)`, 종료 `by=target-recognition` — 선행 0 수정 후 첫 성공 |
| **부족한 축 특정 안내 (FR-009c)** | 13.7s `needMoreObservation(missing: zone)` — D3·D4만 읽어 구역축 부족 → 정확히 특정 |
| **2D 격자 승격 (FR-009d)** | 33.0s 3번째 비공선 관측에서 gridGuidance, 잔차 max 0.22m로 매우 안정 |
| **화살표 부호 (D2)** | 33~35s arrow 36°→3° 수렴 + dist 감소 — **부호 규약 옳음**. ⚠️ 단 후반(67~69s) 해석 정정: 후방 화살표·거리 증가는 "지나침"이 아니라 **목표 추정 자체가 10.2m 어긋나 있었기 때문** (아래 참조) |
| **ZN 파서/스켈레톤** | "D5" 결합형 정상, skeleton-mismatch 11건 기각, 격자 오염 0 |
| **인접 코드 저장 (FR-002b)** | 2차 스캔에서 D6 함께 확정 → neighbors=["D6"] 저장 확인 |
| **스캔 재등록 흐름** | 10:17:09 저장 후 15초 뒤 재등록(대체) 정상 |

## 발견 → 수정 완료

**리플레이어 VM 패리티 버그**: VM은 위치 있는 관측 0개여도 추정을 실행(searching 기록)하는데 리플레이어는 스킵 → 재계산 nil 불일치 3건. 리플레이 계약이 잡아낸 첫 회귀 — 무조건 실행으로 수정(b0915fe). 수정 후 gridUpdated 168건 전부 재계산 일치.

## 🔴 정밀 분석 (260814 추가) — 목표 추정 오차 10.2m → 개선 3종 적용 완료

도착 순간 사용자 실제 위치 (38.5,14.4) vs 추정 목표 (28.4,15.6) = **10.2m 오차** (사용자 체감 "방향·거리 안 맞음" 데이터로 확인).
원인: ① 위치 관측 4점뿐(raycast 실패 88%의 피해) ② 초반 F3/F4 좌표(13~15s)가 47초 뒤까지 잔류 — 62.5m 보행 드리프트 오염 ③ 그 상태에서 신뢰도 high 표시(4점 잔차 0.22m는 자기일관성일 뿐).
→ **개선 커밋 046902b**: 관측 신선도 필터(30s) + 신뢰도 상한(5점 미만 high 금지) + sceneDepth 폴백. 리플레이로 ①~③ 구간이 정직한 안내로 바뀌는 것 검증 완료. sceneDepth 효과는 다음 현장 측정(src 필드로 raycast/depth 비율 기록됨). **책상 스모크 통과(260814)**: 벽 텍스트에서 raycast 실패 → depth fallback hit(d=0.5m) 확인, 앵커 라벨 위치 정상 = 역투영 좌표 변환 검증.

## ⚠️ 추적 항목 — raycast 실패 88%

find 세션에서 **148/168 실패, 최대 연속 48회** (지난번 10/16보다 악화, LiDAR 기기). 관측 11종 중 위치 확보는 4종뿐 — 격자가 4점으로도 잘 됐지만 수렴 속도·강건성을 크게 깎아먹는 병목.
→ **T045 최우선 후보: LiDAR sceneDepth 직접 샘플링 폴백** (raycast 실패 시 텍스트 중심 픽셀의 depth + 카메라 내참으로 unproject). 두 현장 모두에서 재현된 만큼 다음 코드 수정 대상.

## 여전히 미검증

- 강등(D3): 이번 주차장도 규칙적(잔차≤0.22m) — 스네이크 배치 주차장 필요
- 번호 그라디언트 힌트: 강등 미발생이라 발동 안 함 (첫 주차장(번호 단독)에서 재테스트하면 확인 가능)
- 비 LiDAR 기종 (T046)
